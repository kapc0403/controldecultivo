
<html>

<style>
  

       body{
            
            margin-bottom: 10px;  

       }

      .field{

          margin-bottom: 25px;
          text-align: center;
          width: 50%;
          height: 100%;
          

      }


</style>



<body>
  

  <%= form_with(model: farming_student, local: true) do |form| %>
    <% if farming_student.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(farming_student.errors.count, "error") %> prohibited this farming_student from being saved:</h2>

        <ul>
        <% farming_student.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :user_id %>
      <%= form.text_field :user_id, id: :farming_student_user_id, class: "form-control"%>
    </div>

    <div class="field">
      <%= form.label :farming_id %>
      <%= form.text_field :farming_id, id: :farming_student_farming_id, class: "form-control"%>
    </div>

    <div class="actions">
      <%= form.submit style: "border-radius: 10px; color: black; text-align: center; padding: 7px; border: none;
 background: #3a7999;
 color: #f2f2f2;
 padding: 10px;
 font-size: 18px;
 border-radius: 5px;
 position: relative;
 box-sizing: border-box;
 transition: all 500ms ease;
"%>
    </div>
  <% end %>

  </body>

</html>

